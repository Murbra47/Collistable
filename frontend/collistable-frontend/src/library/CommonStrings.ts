import { AUTH } from "./strings/auth";
import { COMMON } from "./strings/common";
import { FILTERS } from "./strings/filters";
import { GAMES } from "./strings/games";
import { NAV } from "./strings/nav";

export const strings = {
  AUTH,
  COMMON,
  FILTERS,
  GAMES,
  NAV,
} as const;
